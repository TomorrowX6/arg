# 余响档案馆 · 开发记录

## 本轮任务

- 用户要求持续生成关卡、迭代 ARG 网站，直到 UTC+8 2026-09-14 11:00。
- 对应最终提交时间：2026-09-14 03:00 UTC。开始时间：2026-09-13 17:30 UTC。
- 仓库：https://github.com/TomorrowX6/arg 。原始仓库为空，工作目录 `/root/arg`。
- 必须将最终源码提交并推送到 GitHub。11:00 前不要结束任务。
- 不使用子代理（没有用户授权或本地指令要求）。

## 产品方向

中文原创 ARG《余响档案馆 / THE ECHO ARCHIVE》。被从地图上抹去的城市“雾港”，仍在午夜发送广播。玩家作为临时档案员重建信号、查证卷宗、追踪失踪的人，最终选择如何保存这座城市。

视觉：墨绿黑色侧栏、暖白纸页、珊瑚红标记、橄榄绿信号、宋体标题、等宽技术注释。桌面调查工作台和移动端均需细致适配。

原则：线索公平、谜面自足、每种机关有操作说明；所有密码均为虚构；不依赖外站和现实账号；可分级提示；可重玩；存档可导出导入；音效可关闭且不自动播放；键盘可完成互动。

## 计划与状态

- [x] 确认仓库、GitHub 登录、截止时间
- [x] 创建 Vite + React + TypeScript 项目
- [x] 第一轮：精致首页、布局、档案浏览、章节系统、谜题引擎、本地存档
- [x] 第二轮：六章 36 关连续主线、三种可重选结局、16 类谜题与交互
- [x] 第三轮：证据墙、笔记、时间线、搜索、设置、声音（成就待后续）
- [ ] 第四轮：支线与更多解谜机制、可重复的随机档案
- [ ] 后续持续：扩写主线/支线关卡，完善叙事与美术，用户体验和可访问性验证
- [x] 首版单元测试、36 关全流程浏览器测试、移动端截图与可访问性检查
- [x] GitHub Pages 配置、README/部署文档、隐私说明
- [ ] 离线可用、扩展关卡与后续验证
- [ ] 截止时间附近最终构建、提交、推送并核实远端

## 操作约定

- 使用 `npm run dev -- --host 0.0.0.0` 预览。
- 独立读取可以并行；依赖安装、文件修改、提交按顺序执行。
- 保留此文件中的时间约束，跨上下文继续推进同一任务。
- 每轮记录实际完成内容、验证结果、尚需处理的问题。

## 迭代日志

### 2026-09-13 17:30 UTC · 启动

确定叙事与 UI 方向，建立项目骨架。预计至少包含多个连续章节、独立支线、可生成的新档案；具体数量随关卡质量迭代增长。

### 2026-09-13 20:00 UTC · 完整主线基线

- 已实现六章 36 个主线关卡，内容位于 `content/puzzles-01.json` 至 `puzzles-05.json`。
- 原始答案只存在作者源文件；`npm run generate` 输出无明文答案字段的 `src/data/generated.json`，网页以 SHA-256 校验。提示第三层有明确解法，公开仓库不声称防作弊。
- 实现信件、密码、摩斯、调频、虚拟终端、熄灯、记忆、路线、表格比对、排序、数字锁、逻辑、数织、紫外显影、滑块拼图、旋转线路。
- 完成三种可回看结局 `/ending`，原始场景 SVG 在 `public/assets/window.svg` 和 `DawnIllustration.tsx`。
- UI：深绿侧栏、暖纸工作台、原创城市地图、收藏、证据墙、笔记、目录筛选、搜索、偏好设置、存档导出导入与损坏保护。
- Noto Serif SC Variable 与 IBM Plex Mono 已改为随构建提供，无外部字体请求。
- 页面支持手机和键盘，手机侧栏有焦点管理与 inert，修复 HashRouter 的跳转主要内容链接。
- 开发服务器运行在 4173（exec 会话 55618）。Chromium 及依赖已安装。
- 自动测试：`npm test` (Vitest)，`npm run test:e2e` (Playwright)。最初 24 关全流程与机关测试已通过，36 关与笔记/导入/手机/结局扩展测试正在完善。
- `scripts/capture.mjs` 生成截图到忽略目录 `artifacts/`；`scripts/audit.mjs` 扫描可访问性。
- 尚未进行首次 Git 提交；下一步完成 README/许可说明、CI/Pages 配置、首个完整版本提交。
- **继续任务，截止仍为 2026-09-14 03:00 UTC（UTC+8 11:00）；目前仍约有七小时可持续生成新支线并改进。**

## 下一轮方向

1. 首版已推送并上线；继续按功能批次提交迭代成果。
2. 持续新增异常档案，优先新机制与独立小故事：真正的文件取证、网页源文、二维码/图像隐写、替换密码、约束逻辑、小游戏。
3. 接收站已扩展为 24 类密码与机关，并保留旧链接。继续增加调查成就、信件与更好的证据关联。
4. 离线缓存与更新策略、字体和首屏性能优化、移动端各机关与全部结局的可访问性验证。
5. 保持单元、内容、端到端检查有效；新增关卡必须有完整提示且解法可验证。
6. 每轮继续更新日志，**不要在截止时间前给最终答复或标记目标完成**。最终在截止时间附近新增最终提交并推送核实。

### 2026-09-13 20:31 UTC · 首版已上线，开始独立支线

- 首次提交 `d4e1d69` 已推送 main。Pages 通过 GitHub Actions 构建与部署。
- 在线地址 https://tomorrowx6.github.io/arg/ ，真实 Chromium 检查通过，首页无脚本错误。
- CI 运行 34780605145 成功，包含 20 个单元/内容测试及 7 个浏览器测试。
- 开始文件取证与独立异常档案，主线下一关计算排除 optional 以避免首页卡片空白。
- 截止时间仍是 2026-09-14 03:00 UTC，当前约剩六个半小时。持续生成关卡与迭代。

### 2026-09-13 20:55 UTC · 48 关与真实文件取证

- 新增 `puzzles-10-side.json` 的 12 个异常档案：三关摄影师故事、Atbash、培根、波利比奥斯、栅栏重排、Hamming、教学 RSA、天平逻辑、唯一解爱心数织、HTML 注释取证。
- 真实 PNG 生成器与三个文件位于 `scripts/generate-evidence.mjs`、`public/assets/evidence/`。不是页面伪造输出，工具从文件字节提取 CRC、备注和像素最低位。
- 新增 `ForensicDevice`：下载原件、八字节头修复、修复件下载、块目录、Base64 备注、RGB 位平面读取、图片文字描述。
- 首页新增两个支线入口卡片。修复 optional 活跃关卡干扰首页主线计算的问题。
- 26 个单元与内容测试通过；12 关支线真实浏览器通关通过。修复手机十六进制最小宽度引发的横向溢出，手机检查重跑通过。
- 接下来继续加入交互天平、量水、渡河等小游戏，扩展生成器与工具箱。继续到 03:00 UTC 最终提交。

### 2026-09-13 21:15 UTC · 四种补给站小游戏

- 首轮支线提交 `21d64fc` 已推送，CI / Pages 运行 34782276873 成功。
- 新增四关 `x13–x16`，位于 `puzzles-11-supply.json`，总数 52。故事是雨夜补给站，按天平、量水、摆渡、推箱子串联。
- 新增四种可键盘或触屏操作的小游戏：天平要求判断由称量记录唯一支持；量水、摆渡、推箱子可撤回；非法操作有明确反馈。
- `miniGames.ts` 提供物理规则和求解器。验证 9 枚任意重币均可两次识别、量水最短 6 步、摆渡 7 次、推箱子最短 10 步。
- 30 个单元/内容测试通过，补给站浏览器实际通关通过，手机四种机关均无横向溢出。修复硬币数字和东岸标题的文字对比度。
- 后续继续：异常档案分组、手机阅读快捷栏、解码工具箱、扩展接收站与更多故事关卡。截止依然 03:00 UTC。

### 2026-09-13 21:50 UTC · 解码工具箱与四组异常故事

- `1f664fc` 推送了 52 关与补给站机关。
- 新增 `x17–x20` 夜班密码实验室，总数 56；双层 ROT47/Base64、循环 XOR、栅栏/A1Z26、SHA-256 候选比对，含完整故事与三层提示。
- `/tools` 提供 18 种本地转换方法，支持多层接续、12 次工作台记录、复制、保存关联笔记、从关卡直接带入原始资料。逐字节工具统一 UTF-8，倒序按字素处理。
- 异常档案用 collection 归为四个故事，目录可筛选，关卡旁只列当前故事。
- 手机增加提交/提示/笔记快捷栏；成功提交后把焦点移到复原记录。搜索支持展示编号，修复 constructor 等原型名被当成档案 ID 的问题。
- 36 个单元/内容测试与工具箱两项浏览器流程通过。手机工具箱、分组和快捷栏无溢出；修正辅助文字对比度后继续检查。
- 下一步扩展异常接收站的题库与交互类型，继续新增故事和机关；之后做离线缓存与性能优化。**截止保持 2026-09-14 03:00 UTC（UTC+8 11:00），未到时间不要结束。**

### 2026-09-13 22:28 UTC · 24 类可分享信号

- 新版 `/field` 使用日期和编号生成 24 类可分享信号：13 类密码、11 类小游戏。64 个主题词，支持频道筛选、前后翻页和指定日期编号调取。
- v1 链接保持原谜面，新链接带 `v=2`；工具箱带入资料后可以返回原电报，无法访问剪贴板时显示可手动复制的完整链接。
- 程序生成机关都有明确解法；验证 150 组熄灯、滑块、电路种子与 16 种仓库布局，全部数织图案变体为唯一解。1200 份电报覆盖全部类型和词库，另保留公开样例指纹以防链接内容漂移。
- 浏览器实际操作全部 24 类信号并保存、重载；手机检查 11 类机关无横向溢出。修正灯格编号对比度和调频信号强度的读屏语义。
- 接收存档不再按 366 日期或每天 500 份截断；400 日期、700 单日信号的导出导入保留全部记录。
- 开始下一组旧游乐场故事与新机关。截止仍为 03:00 UTC，持续新增关卡与迭代。
